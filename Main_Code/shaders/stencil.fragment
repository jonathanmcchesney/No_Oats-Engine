#version 330 core
// Created By Jonathan McChesney
 
// Inputs
in vec2 TextureCoords;

// Outputs
out vec4 fragmentColour;

// Calculate the fragment colour which is a constant colour, - redish colour.
void main()
{             
    fragmentColour = vec4(0.44, 0.12, 0.16, 1.0); // set constant stencil colour - Redish tint
}